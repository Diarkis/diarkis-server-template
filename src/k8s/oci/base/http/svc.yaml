apiVersion: v1
kind: Service
metadata:
  name: http
  annotations:
    oci.oraclecloud.com/load-balancer-type: "lb"
  labels:
    component: http
spec:
  type: LoadBalancer
  selector:
    component: http
  ports:
    - port: 80
      targetPort: 7000
      name: http
      protocol: TCP
